parameters:
    reportUnmatchedIgnoredErrors: true
    ignoreErrors:
        # Replaces checkMissingIterableValueType / checkGenericClassInNonGenericObjectType (removed in PHPStan 2)
        - identifier: missingType.iterableValue
        - identifier: missingType.generics
